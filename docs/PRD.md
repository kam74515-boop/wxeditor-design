# WXEditor — 产品需求文档 (PRD)

> **版本**：v2.0 | **更新日期**：2026-03-16 | **产品形态**：Web SaaS

## 1. 产品概述

**产品名称**：WXEditor（微信公众号图文排版编辑器）

**产品定位**：一款面向自媒体创作者和运营人员的专业富文本排版工具，核心场景方面：
1. 提供高质量的微信公众号文章排版体验
2. 支持多人实时协作编辑
3. AI 辅助写作与文案润色
4. 一键同步到微信公众号草稿箱

**竞品参考**：秀米、135 编辑器

## 2. 目标用户

| 用户类型 | 使用场景 |
|----------|----------|
| **自媒体创作者** | 日常撰写微信推文，利用 AI 辅助提升创作效率 |
| **运营团队** | 多人协同编辑、审阅推文，通过团队功能管理权限 |
| **企业新媒体部门** | 批量管理内容，使用模板库保持品牌一致性 |
| **个人博主** | 快速排版并一键发布到公众号 |

## 3. 核心功能模块

### 3.1 富文本编辑器（基于 UEditor）

- **基础排版**：加粗、斜体、列表、标题、引用、表格等完整富文本操作
- **微信 HTML 适配**：
  - **标签白名单过滤**（sanitize-html）：仅保留微信支持的标签（`p`、`span`、`img`、`section` 等）
  - **内联样式转换**（styleConverter）：将 CSS Class 和外部样式转为行内 `style`
  - **微信属性注入**：为图片自动补充 `data-ratio`、`data-w` 等微信原生属性
- **便利贴风格 UI 设计系统**：波普艺术设计语言，直角边框 + 硬边阴影

### 3.2 AI 写作助手

- **智能对话**：基于通义千问大模型（qwen3.5-plus），支持上下文感知的对话式写作
- **文章润色**：优化措辞和表达，保持原意
- **内容操作**：简化/扩展文章内容
- **标题生成**：为文章生成多个有吸引力的标题
- **摘要生成**：自动生成 120 字以内的公众号摘要
- **代码提取**：自动从 AI 回复中提取 HTML 代码块，可一键插入编辑器

### 3.3 微信公众号对接

- **自动化登录**：Puppeteer 模拟浏览器登录微信公众号后台
- **一键同步**：排版完成后直传公众号草稿箱
- **HTML 复制**：后备方案，一键复制净化后的 HTML 到剪贴板
- **图片代理**：上传图片自动转存微信素材库

### 3.4 多人实时协作

- **WebSocket 实时通信**：基于 Socket.IO 实现毫秒级同步
- **编辑锁控制**：
  - **悲观锁**（默认）：同一时刻仅一人编辑
  - **乐观锁**（可选）：多人同时编辑，版本号自动合并
- **光标共现**：显示其他用户的光标位置与选区
- **即时聊天**：内置聊天面板，边编辑边讨论
- **在线用户**：实时显示协作者列表

### 3.5 模板与素材

- **模板库**：预设排版模板，支持分类筛选与公开/私有
- **素材管理**：图片/视频/音频/文件上传，文件夹管理
- **使用统计**：模板使用次数追踪

### 3.6 团队管理

- **团队创建**：组织级协作空间
- **成员邀请**：邮件/链接邀请机制
- **角色权限**：owner（创建者）、admin（管理员）、member（成员）
- **审计日志**：成员变动记录

### 3.7 用户与会员体系

- **账号体系**：四级权限角色 — `user`、`vip`、`admin`、`superadmin`
- **JWT 认证**：Access Token + Refresh Token 双令牌机制
- **会员套餐**：支持免费/专业/企业等多档套餐
- **结算支付**：支持支付宝、微信支付（预留）

### 3.8 项目管理

- **文档管理**：创建、编辑、归档、删除，支持版本历史
- **文档状态**：draft（草稿）→ published（已发布）→ archived（已归档）
- **多篇文章**：图文消息分组管理

## 4. 非功能性需求

| 需求 | 说明 |
|------|------|
| **架构分离** | 前后端完全分离（Vue 3 SPA + Express API） |
| **轻量存储** | SQLite 单文件数据库，零配置开箱即用 |
| **容器部署** | Docker Compose 一键启动（前端 + 后端 + Nginx） |
| **开发体验** | `npm run dev` 一键并发启动前后端开发环境 |
| **安全防护** | DOMPurify / sanitize-html XSS 防护，文件上传白名单 |
| **图片安全** | Multer 本地转存 或 微信素材库永久链接 |

## 5. 页面路由

| 路由 | 页面 | 认证 |
|------|------|------|
| `/` | 登录页 | 否 |
| `/register` | 注册页 | 否 |
| `/editor` | 编辑器 | 是 |
| `/projects` | 项目管理 | 是 |
| `/templates` | 模板库 | 是 |
| `/components` | 组件库 | 是 |
| `/pricing` | 定价页 | 否 |
| `/checkout` | 结算页 | 是 |
| `/membership` | 会员中心 | 是 |

## 6. 技术栈摘要

| 层 | 技术 |
|----|------|
| 前端 | Vue 3 + TypeScript + Vite 5 + Pinia + Element Plus |
| 编辑器 | UEditor 定制版 |
| AI | 通义千问 qwen3.5-plus（Dashscope API） |
| 后端 | Node.js + Express 5 |
| 实时通信 | Socket.IO |
| 数据库 | SQLite（better-sqlite3） |
| 部署 | Docker Compose + Nginx |
